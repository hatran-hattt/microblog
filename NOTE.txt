1. Simple app
    Source
        microblog/
        app/                -> package 'app'
            __init__.py         -> package 'app' (declare app instance + module 'routes')
            routes.py           -> routes handlers
        microblog.py        -> main application module (import application instanceecho )

    Run app
        set FLASK_APP environment variable -> TODO: check more about config
            (*) Note: The flask run command will look for a Flask application instance in the module referenced by the FLASK_APP environment variable
             Flask allows you to register environment variables that you want to be automatically used when you run the flask command. To use this option you have to install the python-dotenv package.
             Now you can just write the environment variable name and value in a file named .flaskenv located in the top-level directory of the project
        flask run
        flask --app app run
        

2. Template
    The render_template() function invokes the Jinja template engine that comes bundled with the Flask framework.
    Jinja template engine's some feature:
        - fill value
            <h1>Hello, {{ user.username }}!</h1>
        - conditional statements
            <head>
                {% if title %}
                <title>{{ title }} - Microblog</title>
                {% else %}
                <title>Welcome to Microblog!</title>
                {% endif %}
            </head>
        - loop
            {% for post in posts %}
            <div><p>{{ post.author.username }} says: <b>{{ post.body }}</b></p></div>
            {% endfor %}
        - template inheritance (block control statement + extends statement )
            + base template
                {% block content %}{% endblock %} 
            + child template
                {% extends "base.html" %}

                {% block content %}
                    ...
                {% endblock %}

    Note:
        (1) ImportError exception when import module from current directory
            The mockdata module is imported, but it is not clear if it is a local module or an external dependency. If it is a local module, it should follow the proper relative import convention (e.g., from . import mockdata) to avoid conflicts with similarly named external modules.
            Suggested change:
            - import mockdata
            + from . import mockdata


3. Web Forms
    Extensions: Flask-WTF (a thin wrapper around the WTForms package)
    Implement CSRF protection in Flask app
        (1) Set a secret key for your Flask application (set configuration "SECRET_KEY")
            (SECRET_KEY: Flask and some of its extensions use the value of the secret key as a cryptographic key,
            useful to generate signatures or tokens.
            The Flask-WTF extension uses it to protect web forms against a nasty attack called Cross-Site Request Forgery)
        (2) Include the CSRF Token in the Templat: form.hidden_tag()
            - This method primarily handles CSRF protection by injecting the CSRF token as a hidden field.
            - It also iterates through all hidden fields within the form and renders them.
                -> Note: Still define HiddenField to more control and clarity
        (3) Validate form:
            In your Flask view function, validate the form using form.validate_on_submit(). This function checks the CSRF token along with other form data.
        
    Form class (base: FlaskForm): defines the fields of the form as class variables.
        (import (form fields, validator) from wtforms package)
    Form template:
        Fields that are defined in the FlaskForm class know how to render themselves as HTML
            example:
            <form action="" method="post" novalidate>
                {{ form.hidden_tag() }}
                <p>
                    {{ form.username.label }}<br>
                    {{ form.username(size=32) }}<br>
                    {% for error in form.username.errors %}
                    <span style="color: red;">[{{ error }}]</span>
                    {% endfor %}
                </p>
                <p>
                    {{ form.password.label }}<br>
                    {{ form.password(size=32) }}<br>
                    {% for error in form.password.errors %}
                    <span style="color: red;">[{{ error }}]</span>
                    {% endfor %}
                </p>
                <p>{{ form.remember_me() }} {{ form.remember_me.label }}</p>
                <p>{{ form.submit() }}</p>
            </form>

        If you've written HTML web forms in the past, you may have found it odd that there are no HTML fields in this template. This is because the fields from the form object know how to render themselves as HTML. All I needed to do was to include {{ form.<field_name>.label }} where I wanted the field label, and {{ form.<field_name>() }} where I wanted the field. For fields that require additional HTML attributes, those can be passed as arguments. The username and password fields in this template take the size as an argument that will be added to the <input> HTML element as an attribute. This is how you can also attach CSS classes or IDs to form fields.
    View function
        from flask import render_template, flash, redirect

        @app.route('/login', methods=['GET', 'POST'])
        def login():
            form = LoginForm()
            if form.validate_on_submit():
                flash('Login requested for user {}, remember_me={}'.format(
                    form.username.data, form.remember_me.data))
                return redirect('/index')
            return render_template('login.html', title='Sign In', form=form)

        Note
            - form.validate_on_submit()
            - flash(): store message - used to show once on template ({% with messages = get_flashed_messages() %})
            - redirect()


4. Database
    - Extensions:
        + Flask-SQLAlchemy (provides a Flask-friendly wrapper to the popular SQLAlchemy package, which is an Object Relational Mapper or ORM)
            SQLAlchemy supports a long list of database engines, including the popular MySQL, PostgreSQL and SQLite
        + Flask-Migrate (Flask wrapper for Alembic, a database migration framework for SQLAlchemy)
    - Config for Extensions
        + SQLALCHEMY_DATABASE_URI: 
            An environment variable used by Flask-SQLAlchemy to specify the connection string for the database.
            It informs Flask-SQLAlchemy about the database dialect, driver, host, port, username, password, and database name.
            The format of the DATABASE_URL typically follows the pattern: dialect+driver://username:password@host:port/database
            Ex:
                # mysql://user:password@localhost:3306/mydatabase
                # postgresql://user:password@localhost:5432/mydatabase
                # sqlite:////absolute/path/to/database.db

    - Init instances (db = SQLAlchemy(app), migrate = Migrate(app, db))
    - Database Models
        + Import modules (base class: db.Model)
            # sqlalchemy: module includes general purpose database functions and classes such as types and query building helpers
            # sqlalchemy.orm: provides the support for using models
            # typing: support type hints (Optional type)
        + Fields
            # Fields are assigned a type using Python type hints, wrapped with SQLAlchemy's so.Mapped generic type.
                A type declaration such as so.Mapped[int] or so.Mapped[str] define the type of the column,
                and also make values required, or non-nullable in database terms. 
                To define a column that is allowed to be empty or nullable, the Optional helper from Python is also added, as password_hash demonstrates.
            # In most cases defining a table column requires more than the column type.
                SQLAlchemy uses a so.mapped_column() function call assigned to each column to provide this additional configuration.
                In the case of id above, the column is configured as the primary key.
                For string columns many databases require a length to be given, so this is also included.
                I have included other optional arguments that allow me to indicate which fields are unique and indexed,
                which is important so that database is consistent and searches are efficient.

        + Notes:
            # The __repr__ method tells Python how to print objects of this class, which is going to be useful for debugging
            #Note that Flask-SQLAlchemy uses a "snake case" naming convention for database tables by default. For the User model above, the corresponding table in the database will be named user. For a AddressAndPhone model class, the table would be named address_and_phone. If you prefer to choose your own table names, you can add an attribute named __tablename__ to the model class, set to the desired name as a string
    

    - DB Migration
        + [First time only] Create the migration repository
            flask db init
                -> a new migrations directory, with a few files and a versions subdirectory inside. All these files should be treated as part of your project
        + Add/Update model classes
        + flask db migrate (optional: -m "users table")
            -> migration script is generated
                contains 2 function: upgrade, downgrade (This allows Alembic to migrate the database to any point in the history, even to older versions, by using the downgrade path)
        + flask db upgrade/downgrade
            -> SQLite: if db not exist, then auto create
            -> DB Server: need create DB in DB Server before running
    - DB logic
        Add
            db.session.add(modelObj)
            db.session.commit()/rollback()
        Query
            query = sqlalchemy.select(ModelClass)
                                                .orderby(ModelClass.field_name.desc())
                                                .where(ModelClass.field_name.like('a%'))
            query = modelObj.relation_attribute.select()
            db.session.scalars(query)
 ----------------------------------------------


5. User Login
    - Password hashing
        + Package: werkzeug (dependency of Flask -> alredy installed)
            Utility functions:
            werkzeug.security.generate_password_hash
            werkzeug.security.check_password_hash
        + define func in User model: set_password, check_password
    
    - User session management
        + Package: Flask-Login
            It provides user session management for Flask.
            It handles the common tasks of logging in, logging out, and remembering your users’ sessions over extended periods of time.
        + Init instance
            from flask_login import LoginManager
            ...
            login = LoginManager(app)
        + Prepare the User model for flask-login
            The Flask-Login extension works with the application's user model, and expects certain properties and methods to be implemented in it
            The four required items are listed below:
                is_authenticated: a property that is True if the user has valid credentials or False otherwise.
                is_active: a property that is True if the user's account is active or False otherwise.
                is_anonymous: a property that is False for regular users, and True only for a special, anonymous user.
                get_id(): a method that returns a unique identifier for the user as a string
                -> Flask-Login provides a mixin class called UserMixin that includes safe implementations that are appropriate for most user model classes
            
        + User Loader Function
            # Flask-Login keeps track of the logged in user by storing its unique identifier in Flask's user session, a storage space assigned to each user who connects to the application. Each time the logged-in user navigates to a new page, Flask-Login retrieves the ID of the user from the session, and then loads that user into memory.
            # Because Flask-Login knows nothing about databases, it needs the application's help in loading a user. For that reason, the extension expects that the application will configure a user loader function, that can be called to load a user given the ID
            #The user loader is registered with Flask-Login with the @login.user_loader decorator
        + Logging User In
            from flask_login import current_user, login_user
            def login:
                ....if current_user.is_authenticated
                .....login_user(user, boolRememberMe) -> This function will register the user as logged in, so that means that any future pages the user navigates to will have the current_user variable set to that user.

        + Logging User Out
            from flask_login import logout_user
            def logout:
                logout_user()

            (Note: handle showing button login/logout by using current_user.is_anonymous)
        + Requiring User To Login
            # set function handle login for flask-login instance
                login.login_view = 'login'
            # setting to protect views
                @login_required
                -> the decorator is going to redirect to the login page,
                but it is going to include some extra information in this redirect so that the application can then return to the original page
                ex: redirect URL /login?next=/index

                -> setting after call login_user
                    next_page = request.args.get('next')
                    if not next_page or urlsplit(next_page).netloc != '':
                        next_page = url_for('index')
                    return redirect(next_page)

                    ---------
                    If the login URL does not have a next argument, then the user is redirected to the index page.
                    If the login URL includes a next argument that is set to a relative path (or in other words, a URL without the domain portion), then the user is redirected to that URL.
                    If the login URL includes a next argument that is set to a full URL that includes a domain name, then this URL is ignored, and the user is redirected to the index page.
                        -> An attacker could insert a URL to a malicious site in the next argument, so the application only redirects when the URL is relative, which ensures that the redirect stays within the same site as the application. To determine if the URL is absolute or relative, I parse it with Python's urlsplit() function and then check if the netloc component is set or not
                    ----------
        + User Registration
            # RegistrationForm
                from wtforms.validators import ValidationError, DataRequired, Email, EqualTo
                -> Package: email-validator (for Email validator)
                -> custom validator function:
                    + naming: validate_<field_name>
                    + raise: ValidationError


6. Profile Page & Avatar
    - Profile page
        + Route /user/<username>: dynamic component
        + Profile infos: avatar, last seen, about me (TextAreaField)
    - Edit profile page
        + route /edit_profile, handle form.data when re show
            # case post + data valid: do nothing (when redirect to /edit_profile -> case get)
            # case get: fill data from DB
            # case post + data invalid: do nothing (were already populated by WTForms)
    - Others:
        + Use Gravatar service to get avatar
            https://www.gravatar.com/avatar/<MD5 hashed email(lower case)>
            Parameters:
                ?s=128 (avatar image size, default 80)
                ?d=identicon (geometric design image - provided by Gravatar, if user not yet upload image)
            Package md5 hash: from hashlib import md5
        + Jinja sub-template (handle reuse part, ex: post view is shown at profile page + index page)
                {% for post in posts %}
                    {% include '_post.html' %}
                {% endfor %}
        + Recording The Last Visit Time of user
            The @before_request decorator from Flask register the decorated function to be executed right before the view function

# Others
    - Externally Visible Server: flask run --host=0.0.0.0
    - Generating Links: url_for
    - Set configuration: app.config["KEY"] or app.config.from_object, ...
            (app: Flask class's instance)
    

    



